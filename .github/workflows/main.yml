name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Run dummy test
    runs-on: ubuntu-latest

    steps:
      - name: Echo test
        run: echo "CI is working ✅"
